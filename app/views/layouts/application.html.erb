<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title><%= content_for?(:title) ? yield(:title) : "Valéria Parajara" %></title>

  <%= stylesheet_link_tag "application" %>
  <%= csrf_meta_tags %>

  <%= yield(:head) %>
</head>
<body>
  <%= render 'layouts/header' %>
  <%= render 'layouts/menu' %>

  <div class="container-fluid">
    <div class="row">
      <div class="span12">
        <div class="content-header container">
          <div class="row">
            <div class="span6">
              <% if content_for?(:title) %>
              <h1><%= yield(:title) %></h1>
              <% end %>
            </div>
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
  </div>
  <%= render 'layouts/footer' %>
</body>
  <%= javascript_include_tag "application" %>
</html>

